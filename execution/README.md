# Execution

This directory contains deterministic Python scripts used by the agent to perform work.
Scripts here should be reliable, testable, and fast.
Environment variables and API tokens should be stored in the root `.env` file, not hardcoded here.
